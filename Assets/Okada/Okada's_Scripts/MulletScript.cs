using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MulletScript : MonoBehaviour
{
    /*
    [SerializeField] Rigidbody _ballRb;

    [SerializeField] float _force;

    Object_Type _objectType;

    
    void OnCollisionEnter(Collision enterCol)
    {
        
        _objectType = enterCol.gameObject.GetComponent<Object_Type>();

        if (_objectType.Type == ObjectType.Ball)
        {
           _ballRb = enterCol.gameObject.GetComponent<Rigidbody>();
            Vector3 direction = enterCol.transform.position - transform.position;
            _ballRb.AddForce(direction* _force, ForceMode.Impulse); 
           
        }
    }
    */
}
